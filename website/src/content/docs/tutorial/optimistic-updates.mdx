---
title: Optimistic Updates
description: Update shared API data immediately with an optimistic mutation.
sidebar:
  order: 7
---

The previous version of RSVP calls `revalidate(EventKey($slug()))` after the request succeeds. That keeps the detail page correct, but it still waits for the request and only refreshes one cache entry.

For an optimistic update, the attendee count should change before the server responds.

## Entities

There is one more problem to solve before writing the optimistic mutation. The same event record can appear in several cache entries: the detail query, the main list, and filtered lists. Updating only one cache entry would leave the UI inconsistent.

Nano Kit Query solves this with [`entities`](/query/advanced#entities). An entity is a shared cache record that query results can reference. Instead of updating every list and detail cache separately, update the entity once.

Define one [`entity`](/query/advanced#entities) for events:

```ts
import { entities, entity } from '@nano_kit/query'

export const EventEntity = entity<BoardEvent>('event')
```

Map event detail results into that entity:

```ts
query(
  EventKey,
  [$slug],
  fetchEvent,
  [entities(EventEntity)]
)
```

Map every event inside every infinite page too:

```ts
infinite(
  EventsKey,
  [$pacedQ, $category],
  lastPage => lastPage.nextCursor,
  (q, category, cursor) => fetchEvents({ q, category, cursor }),
  [
    entities(data => ({
      ...data,
      pages: data.pages.map(page => ({
        ...page,
        events: page.events.map(EventEntity)
      }))
    }))
  ]
)
```

Expose `$data` from the query client before writing the mutation:

```ts
import { client, infinites, mutations } from '@nano_kit/query'

export const {
  query,
  infinite,
  mutation,
  $data
} = client(
  infinites(),
  mutations()
)
```

Now RSVP can update one entity immediately, then keep it or roll it back:

```ts
export const [rsvp, $rsvpEvent, $rsvpError, $rsvpLoading] = mutation<[id: string], BoardEvent>(
  (id, ctx) => {
    const key = EventEntity(id)
    const previousEvent = $data(key)

    if (previousEvent) {
      $data(key, {
        ...previousEvent,
        attendees: previousEvent.attendees + 1
      })

      onError(ctx, () => {
        $data(key, previousEvent)
      })
    }

    return rsvpEvent(id)
  }
)
```

[`$data`](/query/core-concepts#reading-and-writing-data) updates the normalized entity immediately. [`onError`](/query/advanced#request-context) restores the previous value if the request fails.

`EventEntity(id)` points to one shared event record in cache. RSVP updates that one record, and every query result that references it sees the new attendees count.
